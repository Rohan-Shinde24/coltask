const axios = require('axios');

async function test() {
  try {
    // 1. Get token by logging in (assuming user rohan exists or we can mock)
    // Actually we can't easily do this without a token.
    // Let's check the API directly if possible, or just review the API code again.
    console.log("Cannot test easily without auth token");
  } catch(e) {
    console.log(e);
  }
}
test();
